//http://www.cplusplus.com/reference/cctype/
//#include <climits>// limits of named constants, see page 1284
//#include <cmath> //math library
#include <cctype> // library for strings and other useful validations
#include <iomanip> // library for manipulating output
#include <fstream> // library to direct output to files and get input from files
//#include <cstdlib> // library for the rand and srand functions
#include <ctime> // library to work with time
#include <string> //string library
#include <cstring> //to be able to use strlen
//#include <assert.h> 
#include <iostream>

using namespace std;

void  generateAddition(int& userResponse, int& theCorrectAnswer);
 
int main() 
{
	/* initialize random seed: */
	srand (time(NULL));

	char continu = ' ';
	int userResponse;
	int theCorrectAnswer;

	cout << "y/Y to continue, any other char to exit" << endl;
	cin >> continu;
	system ("cls");
	while (continu == 'y' || continu =='Y')
	{

		generateAddition(userResponse, theCorrectAnswer);
		cin >> userResponse;
		if(userResponse == theCorrectAnswer)
			cout << "correct!\n";
		else
			cout << "incorrect!\n";
		cout << "y/Y to continue, any other char to exit" << endl;
		cin >> continu;
		system ("cls");
	
	}
	return 0;
}
void  generateAddition(int& userResponse, int& theCorrectAnswer)
{
	int num1;
	int num2;
	
	num1 = rand() % 10 + 1; //generate secret number between 1 and 10:
	num2 = rand() % 10 + 1; //generate secret number between 1 and 10:
	theCorrectAnswer = num1 + num2;

	cout << setw(6) << setfill('*') << left << '*' << "ADDITION" << "******" << endl;
	cout << setw(20) << setfill('*') << left << '*' << endl;
	cout << setw(20) << setfill('*') << left << '*' << endl;
	cout << setw(5) << setfill('*') << left << '*' << ' ' << num1 << " + " << num2 << "=? " << setw(6) << right << setfill('*') << '*' << endl;
	cout << setw(20) << setfill('*') << left << '*' << endl;
	cout << setw(20) << setfill('*') << left << '*' << endl;


}